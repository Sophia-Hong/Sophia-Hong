# naholo (Codex용 안내)
이 폴더는 소액사건 나홀로소송 보조 키트다. 사건 이야기가 시작되면 `skills/writing-rules/SKILL.md` 규칙을 먼저 적용하고, 단계별 지시는 `commands/*.md` 를 순서대로 따른다: start → chronology → elements → evidence → fees → draft → efile → review/cite-check → status. 사건 정보는 `case/` 폴더에 저장한다.
