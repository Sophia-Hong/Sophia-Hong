---
description: 상대방 변호사 관점의 반박 시뮬레이션 — 아부성 환각 방지용 스트레스 테스트
---
`writing-rules`, `elements` 스킬과 `case/` 전체를 읽는다. `devil-advocate` 에이전트를 호출해 우리 주장을 가장 강하게 공격하는 답변서 요지를 만들게 하고, 그 결과를 사용자에게 보여준 뒤 "이 반박에 답할 수 있는 증거가 있는가"를 요건별로 묻는다. 결과는 `case/50_opponent/simulated_<날짜>.md` 에 저장한다.
